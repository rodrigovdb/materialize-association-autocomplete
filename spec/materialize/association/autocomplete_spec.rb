# frozen_string_literal: true

RSpec.describe Materialize::Association::Autocomplete do
  it "has a version number" do
    expect(Materialize::Association::Autocomplete::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
